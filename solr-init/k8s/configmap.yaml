# This ConfigMap is created dynamically by the pipeline:
#
#   kubectl create configmap solr-collections-config \
#     --from-file=collections.yml=settings/collections.yml \
#     --namespace solr \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# This file documents the expected resource shape for reference.
apiVersion: v1
kind: ConfigMap
metadata:
  name: solr-collections-config
  namespace: solr
data:
  collections.yml: |
    # Populated at deploy time from settings/collections.yml
